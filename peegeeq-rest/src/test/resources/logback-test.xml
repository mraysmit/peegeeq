<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Test logging configuration for PeeGeeQ REST module -->

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [traceId=%X{traceId:-} spanId=%X{spanId:-} correlationId=%X{correlationId:-}] %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- PeeGeeQ REST specific loggers -->
    <logger name="dev.mars.peegeeq.rest" level="INFO" />
    <logger name="dev.mars.peegeeq" level="INFO" />
    
    <!-- Suppress intentional test failure warnings from REST handlers during tests -->
    <!-- This prevents confusing WARN logs with stack traces during intentional failure tests -->
    <logger name="dev.mars.peegeeq.rest.handlers.ConsumerGroupHandler" level="ERROR" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>
    <logger name="dev.mars.peegeeq.rest.handlers.EventStoreHandler" level="ERROR" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>
    
    <!-- Reduce TestContainers noise -->
    <logger name="org.testcontainers" level="WARN" />
    <logger name="com.github.dockerjava" level="WARN" />
    <logger name="tc" level="WARN" />
    
    <!-- Reduce Vert.x noise -->
    <logger name="io.vertx" level="WARN" />
    
    <!-- Reduce Hikari connection pool noise -->
    <logger name="com.zaxxer.hikari" level="WARN" />
</configuration>
