<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Production logging configuration for PeeGeeQ API module -->
    <conversionRule conversionWord="vxTrace" 
                    converterClass="dev.mars.peegeeq.api.logging.VertxTraceIdConverter" />
    <conversionRule conversionWord="vxSpan" 
                    converterClass="dev.mars.peegeeq.api.logging.VertxSpanIdConverter" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [traceId=%vxTrace span=%vxSpan correlationId=%X{correlationId:-}] %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/peegeeq-api.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/peegeeq-api-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [traceId=%vxTrace span=%vxSpan correlationId=%X{correlationId:-}] %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- PeeGeeQ API module specific loggers -->
    <!--
    Debug logging control for PeeGeeQ API components:
    - Set to DEBUG to see detailed API operation logging
    - Set to INFO to see only important API events
    - Set to WARN to minimize API logging
    -->
    <logger name="dev.mars.peegeeq.api" level="DEBUG" />

    <!-- Specific API component loggers for fine-grained control -->
    <logger name="dev.mars.peegeeq.api.messaging.MessageFilter" level="DEBUG" />
    <logger name="dev.mars.peegeeq.api.setup.DatabaseSetupService" level="DEBUG" />
    <logger name="dev.mars.peegeeq.api.database.DatabaseService" level="DEBUG" />
</configuration>

